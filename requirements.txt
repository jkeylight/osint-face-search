# OSINT Face Search — core dependencies (required)
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
opencv-python-headless>=4.9.0
numpy>=1.26.0
Pillow>=10.0.0
aiohttp>=3.9.0

# --- Optional extras -------------------------------------------------
# Browser-based reverse image engines (Google Lens / Yandex / Bing / TinEye /
# Baidu / SauceNAO web).  After installing, also run:
#   playwright install chromium
#playwright>=1.44

# Higher-accuracy face backend (ArcFace).  Model pack downloads on first use
# and needs internet access to github releases.
#insightface>=0.7.3
#onnxruntime>=1.17

# Testing
pytest>=8.0
httpx>=0.27
